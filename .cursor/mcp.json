{
  "servers": {
    "supabase": {
      "command": "npm",
      "args": ["run", "dev"],
      "cwd": "${workspaceFolder}/tools/supabase-mcp",
      "env": {
        "SUPABASE_ACCESS_TOKEN": "[REDACTED]"
      }
    }
  }
}
